require_relative('card.rb')
class CardGame


  def checkforAce(card)
    if card.value = 1
      return true
    else
      return false
    end
  end

# syntax errors - dif should say def to define the method and there should be a comma between the two parameters
  dif highest_card(card1 card2)
  if card1.value > card2.value
    # logic error - card does not have a name
    return card.name
  else
    card2
  end
end
# syntax error - extra end
end

def self.cards_total(cards)
  # logic error - total is not defined
  total
  for card in cards
    total += card.value
    return "You have a total of" + total
  end
end

# syntax error - missing end to close the class
